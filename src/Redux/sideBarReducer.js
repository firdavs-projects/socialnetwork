const initialState = {
    friends: [
        { id: 1, name: 'Firdavs' },
        { id: 1, name: 'Fedya' },
        { id: 3, name: 'Abdullo' }
    ],
}

const sideBarReducer = (state = initialState, action) => {


    return state
}

export default sideBarReducer